_2026-06-28_

## English

KPM follow-up fixes: KPatch-Next activation handles older target-count return codes, new KPM configs return a normal success code, Android 4.x KPM filtering keeps non-VPN netlink entries visible, and the dashboard ignores disabled native modules when checking .ko/KPM conflicts.

## Русский

Исправления KPM: активация через KPatch-Next поддерживает старый код возврата с числом целей, новые KPM-конфиги возвращают обычный код успеха, фильтрация KPM на Android 4.x сохраняет видимыми не-VPN записи netlink, а dashboard игнорирует выключенные native-модули при проверке конфликта .ko/KPM.
